#!/bin/bash
. /var/local/nodeenv/<%= node[:wercker_devbox][:nodejs] %>/bin/activate

cd /var/local/sites/wercker-pool/src/

echo "wercker-pool npm-update - npm update"
npm update <%= node[:wercker_devbox][:no_bin_links] ? "--no-bin-links" : "" %>

echo "wercker-pool npm-update - npm prune"
npm prune

echo "wercker-pool npm-update - complete"