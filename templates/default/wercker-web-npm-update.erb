#!/bin/bash
. /var/local/nodeenv/<%= node[:wercker_devbox][:nodejs] %>/bin/activate

cd /var/local/sites/wercker/src/

echo "wercker-web npm-update - npm update"
npm update <%= node[:wercker_devbox][:no_bin_links] ? "--no-bin-links" : "" %>

echo "wercker-web npm-update - npm prune"
npm prune

echo "wercker-web npm-update - complete"