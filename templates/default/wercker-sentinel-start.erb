#!/bin/bash
echo -ne '\033]0;wercker-sentinel\007'
sudo bash -c '. /var/local/nodeenv/<%= node[:wercker_devbox][:nodejs] %>/bin/activate &&
<% if node[:wercker_devbox][:use_supervisor] -%>
supervisor coffee /var/local/sites/wercker-sentinel/src/index.coffee
<% else -%>
coffee /var/local/sites/wercker-sentinel/src/index.coffee
<% end -%>'
